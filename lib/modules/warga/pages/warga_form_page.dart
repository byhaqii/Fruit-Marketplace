import 'package:flutter/material.dart';

class WargaFormPage extends StatelessWidget {
  const WargaFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Warga Form')),
      body: const Center(child: Text('Warga Form')),
    );
  }
}
