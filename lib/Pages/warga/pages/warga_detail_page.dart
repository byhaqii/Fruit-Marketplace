import 'package:flutter/material.dart';

class WargaDetailPage extends StatelessWidget {
  const WargaDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Warga Detail')),
      body: const Center(child: Text('Warga Detail')),
    );
  }
}
